<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rudyrachman16.dailytriviaquiz">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ApplicationSetting"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DailyTriviaQuiz">
        <activity
            android:name=".ui.ResultActivity"
            android:exported="true"
            android:theme="@style/Theme.DailyTriviaQuiz.NoActionBar" />
        <activity
            android:name=".ui.ListQuizActivity"
            android:exported="true"
            android:theme="@style/Theme.DailyTriviaQuiz.NoActionBar" />
        <activity
            android:name=".ui.quiz.normal.ui.QuizActivity"
            android:exported="true"
            android:label="@string/title_activity_quiz"
            android:theme="@style/Theme.DailyTriviaQuiz.NoActionBar" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DailyTriviaQuiz.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>